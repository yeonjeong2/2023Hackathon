spring:
  datasource:
    url: jdbc:mysql:tcp://127.0.0.1:3306/~/hackathon?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&autoReconnect=true
    username: root
    password: 'Abcd123!'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true